---
title: "R Notebook"
output: html_notebook
---

```{r}
datasets::trees
```

```{r}
names(trees)
```
```{r}
cor(trees)
```
```{r}
par(mfrow = c(3, 1))
plot(trees$Girth, trees$Volume)
plot(trees$Girth, trees$Height)
plot(trees$Volume, trees$Height)
```

```{r}
lm.p<-lm(trees$Volume~trees$Girth + trees$Height)
summary(lm.p)
```


```{r}
plot(lm.p)
```

```{r}
# trees$Volume = 57.9877 + 4.7082  * trees$Height
```

```{r}
r1<-residuals(lm.p)
data.frame(r1)
d1<-data.frame(trees$Volume,predict(lm.p),r1)
d1
```

    4. Проведите регрессионный анализ для определения существования линейной зависимости между объемом вишневых деревьев(y) и диаметром(x1) c высотой (x2). Запишите уравнение зависимости с полученными коэффициентами регрессии. Выведите график зависимости.
    5. Чему равен полученный коэффициент детерминации? Поясните смысл полученного численного значения.
    6. Проведите анализ остатков для проверки адекватности построенной регрессионной модели.
    7. Сделайте вывод по результатам проведенного регрессионного анализа.
    8. Откройте таблицу Turtle.csv c данными по длине, ширине, высоте и весу черепах.
    9. С помощью теста Шапиро-Уилка проверьте на нормальность каждый столбец таблицы. Сделайте выводы.
    10. Постройте корреляционную матрицу по данным таблицы Turtle. Определите значимые зависимости.
    11. Проведите регрессионный анализ для определения существования линейной зависимости между весом (weight) черепах и другими параметрами (length, width, height). Сделайте вывод по результатам анализа.
